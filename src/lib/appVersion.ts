// Bump these together with android/app/build.gradle (versionName / versionCode)
// and ios Info.plist for every new mobile release.
export const APP_VERSION = '3.2';
export const APP_BUILD = 6;
export const APP_VERSION_LABEL = `v${APP_VERSION} (build ${APP_BUILD})`;
